const context: any = {}
const CommonPrompts = {
  WRITE_EMAIL: `You are a ${context.role} at a ${context.company} and you are writing an email to a ${context.recipient} to ${context.action}. \n\n`,
}
